import { Input } from '@chakra-ui/react'
import type { InputProps } from '@chakra-ui/react'
import React from 'react'

const RoomInput: React.FC<InputProps> = (props) => <Input {...props} />

export default RoomInput
