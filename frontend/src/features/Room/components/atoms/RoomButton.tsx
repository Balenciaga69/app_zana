import { Button } from '@chakra-ui/react'
import type { ButtonProps } from '@chakra-ui/react'
import React from 'react'

const RoomButton: React.FC<ButtonProps> = (props) => <Button {...props} />

export default RoomButton
